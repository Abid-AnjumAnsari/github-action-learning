name: My First work flow

on: [push]

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
        -  name: checkout step
           uses: actions/checkout@v6
        -  name: Welcome Message
           run: echo "My first Github action job"

        -  name: List files
           run: ls

        -  name: Read file
           run: cat README.md
        -  name: checking current path
           run: pwd
        